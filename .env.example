# Uso local (Postgres na sua máquina)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=controle_gastos
DB_PASSWORD=
DB_PORT=5432

# Deploy (ex: Neon/Vercel) — se DATABASE_URL estiver definida, ela tem prioridade
# sobre as variáveis DB_* acima
DATABASE_URL=

PORT=3000
JWT_SECRET=
